# Python 依赖
# 安装方式: pip install -r requirements.txt

httpx>=0.28          # HTTP 客户端，用于调用新浪财经和东方财富 API
markdown>=3.10       # Markdown 转 HTML，用于生成买卖建议报告
playwright>=1.59     # 浏览器自动化，用于 Markdown 报告截图转 PNG（使用内置 Chromium）
openpyxl>=3.1.5      # 用于使用excel作为默认配置文件


# ⚠️ 注意：playwright 使用内置 Chromium 浏览器，无需系统预装 Chrome
# 若内置 Chromium 不可用，执行: playwright install chromium
